import 'main.dart';
import 'package:flutter/material.dart';
// Costumed widget/Icon for body item that display item's price

class food_description extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return FlatButton(
      onPressed: () {},
      child: Text("Rwf 2500"),
      color: buttoncolour,
    );
  }
}
